/**
 * Barrel para imports `@/types` (Fase 8.1). Tipos canónicos em `models.ts`.
 */
export type {
  AiAnalysis,
  AiInsight,
  AuditLog,
  AuthResponse,
  FunnelEvent,
  Intent,
  Lead,
  LeadStatus,
  LeadWithMessages,
  Message,
  MessageItem,
  MessagesResponse,
  PaginatedResponse,
  Role,
  Sentiment,
  Source,
  User,
} from './models';
